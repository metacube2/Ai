# Handoff 2026-06-16

Zweck: Dieses Dokument zuerst laden, wenn der Chat neu gestartet wird. Es fasst den aktuellen Stand fuer TrafagSalesExporter / BiDashboard kompakt zusammen.

Nachtrag 2026-06-18: Es gibt eine neue Management-/Roadmap-Doku fuer Ingos naechste 180 Tage:

```text
docs/INGO_TODOS_180_TAGE_2026-06-18.md
docs/INGO_TODOS_180_TAGE_2026-06-18.docx
```

Inhalt: Sales Management Cockpit/Data-Lake als Prioritaet 1, HR Dashboard und Einkaufs Dashboard als Prioritaet 2/3, Q3/Q4-Meilensteine, Abhaengigkeiten, Risiken und konkrete naechste 10 Schritte. Abgrenzung: S/4HANA Compatibility Check/RPC-/RFC-Themen bleiben bei Lucas; Infrastruktur/Security/Server/Netzwerk bei Alex/Ramon/Upgreat; Ingo bleibt bei Analytics, BI, Reporting-/Z-Funktionsbezug und .NET/ASP-Webseiten.

## Lade-Reihenfolge im neuen Chat

1. `docs/HANDOFF_2026-06-16.md`
2. `lastchange.md`
3. Je nach Thema:
   - Finance / Budget-CHF: `docs/rag/FINANCE.md`
   - Produktsparten / OData: `docs/rag/PRODUCT_MAPPING.md`
   - Router: `docs/RAG_ROUTER.md`

## Letzte Commits

```text
4f45805 Improve purchasing dashboard matrix
821f5a4 Add Budget CHF multiple choice questionnaire
e0d89b7 Document open Budget CHF finance questions
9f4db97 Guard product division OData refresh
918969e Prepare product division reference mapping
09afce2 Document OData dashboard context
bb47163 Add Alphaplan full delta import
```

## Wichtigster Code-Stand

- Einkaufsdashboard 2026-06-18:
  - Excel-aehnliche Lieferant/Jahr-Kaskadierung fuer 2020 bis aktuelles Jahr.
  - Spend aktuelles Jahr je Lieferant, Top-down sortiert.
  - Balken-/KPI-Sicht fuer bereits beschafft/gebucht und offene Bestellungen/Zulauf.
  - Filter fuer Loeschkennzeichen und MARA-MSTAE.
  - Lieferantennamen statt Platzhalter, soweit Quelle vorhanden.
  - Aktive Lieferanten werden auf reale Einkaufsbewegungen bezogen.
- HR KPI Zugang:
  - Alter HR-User `hr` bleibt unveraendert.
  - `HrKpiAccess.AdminUsers` ermoeglicht zusaetzliche HR-Admin-Logins.
  - Aktueller Zusatzuser ist `hradmin`; Passwort wurde separat kommuniziert, nur der Hash ist in `appsettings.json` gespeichert.
- Finance 3D:
  - Neue Diagrammart `Sparten-Kreis je Land`.
  - Je Land werden Produktsparte-Sektoren aus der zentralen Spartenzuordnung dargestellt.
- Gruppenmarge:
  - Neuer Reiter `Management Analyse > Experten > Gruppenmarge`.
  - Neuer Navigationspunkt und Schnelluebersicht-Button.
  - Doku: `docs/FINANCE_GRUPPENMARGE_2026-06-16.md`.
  - Multiple Choice: `docs/FINANCE_GRUPPENMARGE_MULTIPLE_CHOICE_2026-06-16.docx`.
  - Status: MVP/Pruefsicht, kein finaler Finance-Abschlusswert.
  - Marge und Prozent werden nur angezeigt, wenn die Kostenbasis vollstaendig geklaert ist; bei `Standardpreis fehlt` oder `Lieferant unklar` wird `-` angezeigt.
  - Aktueller Befund: AT/TRAT und CH/TRCH haben in den geprueften 2025-Zentraldaten `StandardCost=0` und leere Supplier-Felder; Gruppenmarge bleibt dort offen.
- Dashboard-Nachweis-Excel:
  - `Zentrale Datei neu erzeugen` erstellt neben `Sales_All_<yyyy-MM-dd>.xlsx` auch `Finance_Dashboard_Nachweis_<yyyy-MM-dd>.xlsx`.
  - Ablage ist derselbe waehlbare lokale Ordner wie fuer die zentrale Datei: `LocalConsolidatedExportFolder`, Fallback `LocalSiteExportFolder`, Fallback `output`.
  - Die Nachweis-Datei enthaelt Detailblaetter und Formel-Summaries fuer Finance, Soll/Ist, Sparten, Gruppenmarge und Datenqualitaet.
  - Doku: `docs/FINANCE_DASHBOARD_NACHWEIS_2026-06-17.md`.
- Produktsparten-Mapping ist lokal/committed auf den neuen SAP-OData-Referenzservice vorbereitet.
- `P = ProductDivisionRefSet` bleibt Alias und fuehrende Quelle.
- `M = ProductDivisionMapSet` bleibt als Rueckfallkonfiguration im Seed vorhanden, ist aber inaktiv.
- Alter Join `Z.Prodh = M.Paph1` ist inaktiv.
- Produktfelder kommen direkt aus `P.*`:
  - `P.Paph1`, `P.Paph1Text`
  - `P.Wwpfa`, `P.WwpfaText`
  - `P.Wwpsp`, `P.WwpspText`
  - `P.IsAssigned`
- OData-Join normalisiert `Matnr` auf beiden Seiten:
  - Trim
  - ToUpperInvariant
  - Whitespace entfernen
  - fuehrende Nullen entfernen
- Neue Kategorie `Uebrige` fuer `ProductDivisionCode = 0008`; getrennt von `Nicht zugeordnet` und `Nicht im TR-AG-Stamm`.
- Guardrails:
  - grosse ProductDivisionRefSet-Referenz mit 0 assigned bricht Import ab.
  - SAP-Fetch mit 0 Sales Records bricht ab, damit CentralSalesRecords nicht leer ueberschrieben wird.
  - SAP Gateway Timeout auf 5 Minuten erhoeht.

## Tests

Letzter voller Testlauf nach Codeaenderungen:

```text
dotnet test TrafagSalesExporter.sln --verbosity minimal
101/101 Tests gruen
```

Der vorherige zusaetzliche Finance-Test prueft die erzeugte `Finance_Dashboard_Nachweis_<yyyy-MM-dd>.xlsx` inkl. Blattstruktur und Kernformeln. Der aktuelle Lauf vom 2026-06-18 umfasst zusaetzlich die Einkaufsdashboard-Aenderungen.

## Deploy 2026-06-18

- Publish-Ziel: `\\trch-webapp-bidashboard.trafagch.local\BiDashboard$\`.
- Deploy erfolgte mit kurzem `app_offline.htm`.
- `app_offline.htm` wurde nach Publish entfernt.
- Erreichbarkeit: `Test-NetConnection trch-webapp-bidashboard.trafagch.local -Port 443` mit `TcpTestSucceeded : True`.
- Produktive DLL: `BiDashboard.dll` Zeitstempel `18.06.2026 09:29:11`.

## SAP/OData Live-Checks

Konfigurierte Dashboard-Service-URL:

```text
http://travp762.sap.trafag.com:8000/sap/opu/odata/sap/ZPOWERBI_EINKAUF_SRV/
```

### ProductDivisionRefSet

URL:

```text
ProductDivisionRefSet?$format=json
```

Letzter Check nach SAP-Fix:

```text
HTTP 200 OK
Rows=48897
AssignedFlag=48895
Misc0008=8715
Unass=2
EmptyWwpsp=0
```

Echte UNASS-Beispiele:

```text
16618  8950 DRUCKTRANSMITTER ECOTRANS  Paph1=8950  Wwpsp=UNASS  IsAssigned=False
24432  8950 DRUCKTRANSMITTER ECOTRANS  Paph1=8950  Wwpsp=UNASS  IsAssigned=False
```

Feldcheck P:

```text
Matnr      leer 0
Maktx      leer 6912
Paph1      leer 6396
Paph1Text  leer 7041
Wwpfa      leer 6398
WwpfaText  leer 2
Wwpsp      leer 0
WwpspText  leer 0
IsAssigned leer 0
```

Interpretation:

- `Wwpsp` ist voll gefuellt.
- `Uebrige` kommt sauber ueber `Wwpsp=0008`.
- Leere Familie ist bei `0008` fachlich erlaubt.
- Nur 2 echte `Nicht zugeordnet`-Referenzen bleiben in SAP.

### FinanzdataSchweizOeSet

Counts:

```text
FinanzdataSchweizOeSet/$count                         -> 30642
FinanzdataSchweizOeSet/$count?$filter=Gjahr eq '2025' -> 30642
FinanzdataSchweizOeSet/$count?$filter=Gjahr eq '2026' -> 0
```

JSON-Zeilenabfrage:

```text
FinanzdataSchweizOeSet?$format=json                         -> Rows=30642
FinanzdataSchweizOeSet?$format=json&$filter=Gjahr eq '2025' -> Rows=30642
FinanzdataSchweizOeSet?$format=json&$filter=Gjahr eq '2026' -> Rows=0
```

## Refresh-Status

- Nach den erfolgreichen OData-Checks wurde der Dashboard-Import/Refresh noch nicht gestartet.
- Naechster operativer Schritt:
  1. aktuelle Commits deployen oder App lokal starten, damit Seed laeuft,
  2. sicherstellen, dass `M` inaktiv und direkte `P.*`-Mappings aktiv sind,
  3. ZSCHWEIZ-Refresh starten,
  4. danach Spartenabdeckung im Dashboard pruefen.
- Wichtig: In einer lokalen DB vor Seed/App-Start kann noch alte Konfiguration sichtbar sein:
  - `M ProductDivisionMapSet Active=True`
  - `Z.Prodh = M.Paph1 Active=True`
  - `FirstNonEmpty(P.*, M.*)`
  Nach Seed muss das korrigiert sein.

## Finance / Budget-CHF

Bereits fachlich dokumentiert:

- Finance Summary bleibt Local Currency / Hauswaehrung je Land.
- Budget-CHF ist nur Zusatz-/Kontrollsicht.
- Keine Tages-/SNB-/ECB-Kurse fuer Budget-CHF.
- Kursjahr soll `Finance | Year` sein.

Neue Dateien:

```text
docs/FINANCE_BUDGET_CHF_FRAGEN_FINANZCHEF_2026-06-15.md
docs/FINANCE_BUDGET_CHF_MULTIPLE_CHOICE_2026-06-16.docx
```

Wichtiges Audit-Ergebnis:

- Zentrale Finance Summary und `Sales_All_2026-06-11.xlsx` rechnen nicht still um; sie bleiben in Hauswaehrung.
- Budgetkurse 2025/2026 existieren in `CurrencyExchangeRates`.
- Offene ECB-Kurse koennen die normale CHF-Anzeige beeinflussen, z.B. `EUR->CHF` 2026 wurde als `0.923` aus ECB statt Budget `0.94` aufgeloest.
- Fuer eine kuenftige Spalte `Finance | Net Sales Actual CHF Budget` daher explizit nur `Notes = Budget <FinanceYear>` verwenden.

Offene Finance-Entscheide:

- Budgetkurse je Jahr verbindlich freigeben.
- Owner/Pflegeprozess bestimmen.
- Spaltenumfang bestaetigen.
- Fehlender Budgetkurs: leer + Missing markieren vs. 0 vs. Exportabbruch.
- Rundung.
- Anzeigeort im Excel/Dashboard.
- DE/Alphaplan 2026: ab wann Finance-Jahr 2026 statt erzwungen 2025?
- Kontrollnachweis fuer Review.

## Alphaplan / DE

- Full + Delta Import ist committed.
- Dateien:
  - `invoice_headers.csv`
  - `invoice_lines.csv`
  - `delta/invoice_headers.csv`
  - `delta/invoice_lines.csv`
- Delta gewinnt gegen Full bei gleicher Zeile.
- `ArtikelNummer` aus Alphaplan ist lokale Materialnummer und nicht garantiert TR-AG-/SAP-`MATNR`.
- Fuer Spartenabdeckung DE kann ein separates Nummern-/Mappingthema entstehen.
- Aktuelle Finance-Regel zwingt DE noch auf Finance-Jahr 2025.

## Dirty Worktree / Nicht anfassen ohne Rueckfrage

Beim letzten Status gab es viele untracked/exportierte Dateien und zwei geloeschte Bilder:

```text
D Bild.png
D erg.png
?? AlphaplanExportPackage/delta/
?? AlphaplanExportPackage/invoice_headers.csv
?? AlphaplanExportPackage/invoice_lines.csv
?? docs/Sales_TRDE_2026-06-*.xlsx/csv
?? spartenlogic/*
?? x.pbix
```

Diese Dateien wurden nicht gestaged und nicht bereinigt. Nur gezielte Doku-Dateien wurden committed.

## Empfohlener naechster Einstieg

Wenn der naechste Chat weiterarbeiten soll, zuerst klaeren:

```text
Willst du als naechstes deployen/importieren, oder zuerst nur noch pruefen?
```

Wenn deploy/import:

1. `git status --short` pruefen.
2. Nur relevante Dateien anfassen, untracked Exportdateien ignorieren.
3. `dotnet test TrafagSalesExporter.sln --verbosity minimal` ggf. erneut laufen lassen.
4. Publish/Deploy nach bestehendem IIS-Prozess.
5. App starten/Seed pruefen.
6. ZSCHWEIZ Refresh.
7. Danach Dashboard-Spartenstatus kontrollieren:
   - `Zugeordnet`
   - `Uebrige`
   - `Nicht zugeordnet`
   - `Nicht im TR-AG-Stamm`
